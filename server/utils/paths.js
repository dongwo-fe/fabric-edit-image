const path = require('path');

const ImageListPath = path.resolve('data/imageData.json');
const ImageFilePath = path.resolve('public/images');
const ImageDetailPath = path.resolve('data/imageDetail.json');

module.exports = {
  ImageListPath,
  ImageFilePath,
  ImageDetailPath
}
